document.addEventListener('DOMContentLoaded', () => {let randomNumber = Math.floor(Math.random() * 200) + 1;let attempts = 5;const guessInput = document.getElementById('guessInput');const guessBtn = document.getElementById('guessBtn');const message = document.getElementById('message');const attemptsLeft = document.getElementById('attemptsLeft');const resetBtn = document.getElementById('resetBtn');function handleGuess() {const userGuess = parseInt(guessInput.value);if (isNaN(userGuess) || userGuess < 1 || userGuess > 200) {message.innerText = 'ניחוש מספר בין 1 ל-200';return;}attempts--;if (userGuess === randomNumber) {message.innerText = '!!!';message.style.color = '#3ae660';guessInput.disabled = true;guessBtn.style.display = 'none';resetBtn.style.display = 'inline-block';} else if (attempts === 0) {message.innerText = `המספר היה ${randomNumber}`;message.style.color = '#ebd4d4';guessInput.disabled = true;guessBtn.style.display = 'none';resetBtn.style.display = 'inline-block';} else {message.innerText = 'ניסיונות נשארו: ' + attempts;}};guessBtn.addEventListener('click', handleGuess);resetBtn.addEventListener('click', () => {randomNumber = Math.floor(Math.random() * 200) + 1;attempts = 5;attemptsLeft.innerText = attempts;message.innerText = '';message.style.color = '#4b6584';guessInput.disabled = false;guessInput.value = '';guessBtn.style.display = 'inline-block';resetBtn.style.display = 'none';});guessInput.addEventListener('keypress', (e) => {if (e.key === 'Enter') {handleGuess();}});});