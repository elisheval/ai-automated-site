document.addEventListener('DOMContentLoaded', () => {let randomNumber = Math.floor(Math.random() * 200) + 1;let attempts = 5;const guessInput = document.getElementById('guessInput');const guessBtn = document.getElementById('guessBtn');const message = document.getElementById('message');const attemptsLeft = document.getElementById('attemptsLeft');const restartBtn = document.getElementById('restartBtn');function handleGuess() {const userGuess = parseInt(guessInput.value);if (isNaN(userGuess) || userGuess < 1 || userGuess > 200) {message.innerText = 'ניחש מספר בין 1-200';return;}attempts--;attemptsLeft.innerText = attempts;if (userGuess === randomNumber) {message.innerText = 'מצוין!';message.style.color = '#3ae660';guessInput.disabled = true;guessBtn.style.display = 'none';restartBtn.style.display = 'inline-block';} else if (attempts === 0) {message.innerText = `המספר היה ${randomNumber}`;message.style.color = '#eb4d4b';guessInput.disabled = true;guessBtn.style.display = 'none';restartBtn.style.display = 'inline-block';} else if (userGuess > randomNumber) {message.innerText = 'גבוה מדי';} else {message.innerText = 'נמוך מדי';}guessInput.value = '';guessInput.focus();}guessBtn.addEventListener('click', handleGuess);restartBtn.addEventListener('click', () => {randomNumber = Math.floor(Math.random() * 200) + 1;attempts = 5;attemptsLeft.innerText = attempts;message.innerText = '';message.style.color = '#4b6584';guessInput.disabled = false;guessInput.value = '';guessBtn.style.display = 'inline-block';restartBtn.style.display = 'none';});guessInput.addEventListener('keypress', (e) => {if (e.key === 'Enter') {handleGuess();}});});